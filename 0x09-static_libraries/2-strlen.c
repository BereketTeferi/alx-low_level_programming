#include "main.h"
#include <string.h>

/**
 * _strlen - compute length of a string
 *
 * @s: string to be calculated
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
