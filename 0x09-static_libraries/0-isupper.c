#include "main.h"

/**
 * _isupper - checks upper case
 *
 * @c: to be checked
 *
 * Return: 1 if true
 * 0 else
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
