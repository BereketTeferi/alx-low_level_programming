#include "main.h"

/**
 * _isdigit - checks if a character is digit
 * @c: to be checked
 *
 * Return: 1 if true
 * 0 otherwise
 */

int _isdigit(int c)
{
	if (isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
