#include "main.h"
#include <stdlib.h>

/**
 * _abs - make absolute value
 *
 * @n: evaluated by absolute value
 *
 * Return: always 0 if success
 */

int _abs(int n)
{
	_putchar(abs(n));

	return (0);
}
