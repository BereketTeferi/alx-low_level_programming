#include "main.h"
#include <string.h>

/**
 * _strcpy - clone of strcpy
 *
 * @dest: destination of string
 * @src: source of string
 *
 * Return: destination
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
