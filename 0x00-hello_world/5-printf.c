#include <stdio.h>

/**
 * main - prints using printf
 *
 * Description - Prints with proper grammar, but the outcomen...
 *
 * Return: 0 on success, non zero on failure
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
